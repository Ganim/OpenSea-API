# ==================================
# OPENSEA API - Environment Variables
# ==================================

# Ambiente (dev, test, production)
NODE_ENV=dev

# Porta do servidor
PORT=3333

# ==================================
# AUTENTICAÇÃO JWT
# ==================================
# ATENÇÃO: Use uma chave forte e única em produção!
# Exemplo: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# ==================================
# BANCO DE DADOS
# ==================================
# PostgreSQL connection string
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL=postgresql://docker:docker@localhost:5432/opensea-db?schema=public

# ==================================
# SMTP (Email)
# ==================================
# Para testes, use: https://ethereal.email/
# Para produção, use: Gmail, SendGrid, AWS SES, etc.
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# ==================================
# FRONTEND (CORS)
# ==================================
# URL do frontend para configuração CORS
# Em produção, use a URL real do seu frontend
FRONTEND_URL=http://localhost:3000

# ==================================
# REDIS
# ==================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ==================================
# JWT RS256 (opcional - fallback para HS256)
# ==================================
# Para gerar as chaves:
#   openssl genrsa -out keys/private.pem 2048
#   openssl rsa -in keys/private.pem -pubout -out keys/public.pem
# Depois cole o conteúdo das chaves aqui (em uma única linha, substituindo quebras de linha por \n)
# JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----"
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nMIIB...\n-----END PUBLIC KEY-----"

# ==================================
# SENTRY (opcional - monitoramento de erros)
# ==================================
# Crie um projeto em https://sentry.io e copie o DSN
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ==================================
# AUDIT HMAC (opcional - integridade de logs)
# ==================================
# Chave secreta para assinar logs de auditoria
# Gere com: openssl rand -base64 32
# AUDIT_HMAC_SECRET=your_hmac_secret_key_here