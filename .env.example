# ==================================
# OPENSEA API - Environment Variables
# ==================================

# Ambiente (dev, test, production)
NODE_ENV=dev

# Porta do servidor
PORT=3333

# ==================================
# AUTENTICAÇÃO JWT
# ==================================
# ATENÇÃO: Use uma chave forte e única em produção!
# Exemplo: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# ==================================
# BANCO DE DADOS
# ==================================
# PostgreSQL connection string
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL=postgresql://docker:docker@localhost:5432/opensea-db?schema=public

# ==================================
# SMTP (Email)
# ==================================
# Para testes, use: https://ethereal.email/
# Para produção, use: Gmail, SendGrid, AWS SES, etc.
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# ==================================
# FRONTEND (CORS)
# ==================================
# URL do frontend para configuração CORS
# Em produção, use a URL real do seu frontend
FRONTEND_URL=http://localhost:3000