/**
 * Phase 9 / Plan 09-02 — E2E spec stub for v1-get-audit-detail.
 */

import { describe, it, expect } from 'vitest';

describe('GET /v1/hr/punch/audit/:id (E2E stub — Phase 09-02)', () => {
  it.skip('returns 404 for non-existent ID', async () => {
    // TODO: GET non-existent → 404
  });

  it.skip('returns audit detail + signals + map', async () => {
    // TODO: Setup time entry, GET detail, verify signals
  });
});
